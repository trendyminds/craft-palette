/*
 * Preflight is deliberately omitted — Palette renders inside the Craft control
 * panel and must not reset the host page's styles.
 */
@layer theme, base, components, utilities;
@import 'tailwindcss/theme.css' layer(theme) prefix(p);
@import 'tailwindcss/utilities.css' layer(utilities) prefix(p) important;

@source '../scripts/**/*.{js,jsx}';

@theme {
	/* Replace the default breakpoints rather than extending them */
	--breakpoint-*: initial;
	--breakpoint-xs: 480px;
	--breakpoint-sm: 640px;
	--breakpoint-md: 768px;
}

/* Ensure fonts aren't replaced by a site's custom one */
[data-palette] * {
	@apply p:font-sans;
}
